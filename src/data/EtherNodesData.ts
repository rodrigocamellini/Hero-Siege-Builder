export const rawData: Array<{ x: number; y: number; name?: string; description?: string }> = [{ x: 0, y: 0, name: 'Root', description: '' }];

export const connections: Array<{ from: number; to: number }> = [];

export const CENTER_ORIGIN = { x: 0, y: 0 };

export const backgroundImages: Array<{ id?: string; name?: string; image?: string; x: number; y: number; width?: number; height?: number; opacity?: number }> = [];

